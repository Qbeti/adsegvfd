apiVersion: v1
kind: Service
metadata:
  name: adsegvfd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "adsegvfd"
    port: 8080
  selector:
    app: adsegvfd
